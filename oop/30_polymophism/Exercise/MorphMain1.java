/* Write a main class named MorphMain. In the main method:
 *
 * create an instance of Morph1 named morph1.
 * Create a vairiable called target of type Object initialize it to null.
 * Assign the instance of Morph1 to variable target. */

public class MorphMain1 {
    public static void main(String[] args) {
        Morph1 morph1 = new Morph1();  // Creating an instance of Morph1 named morph1

        Object target = null;  // Creating a variable called target of type Object and initializing it to null

        target = morph1;  // Assigning the instance of Morph1 to the variable target
    }
}

/* In the main method, we create an instance of Morph1 named morph1. 
 * Then, we declare a variable called target of type Object and initialize it to null. 
 * Finally, we assign the instance of Morph1 (morph1) to the target variable. */


